name: playground
description: Minimal Homeric editor smoke test.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  homeric:
    path: ../../packages/homeric

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
